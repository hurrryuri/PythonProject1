"""
다중주석, 문자열 값
"""
#콘솔 프로그램은 표준입출력 명령
#입력 : input(), scanner(java), scanf(C++)
#출력 : print(), print(java), printf(c++)
#변수는 데이터형이 존재하지 않는다. javascript처럼 들어오는 값이 데이터형을 결정
#변수명, let 변수명(javascript), Integer 변수명(java), int변수명(C++)

#문법 규칙 : 블럭지정이 없다. 블럭은 들여쓰기로 구분한다. tab으로 들여쓰기 하기
a = input() #키보드로 입력받은 값을 a에 저장, 모든 입력은 문자로 처리
b = input()

result = a + b #연산

print(result)  #콘솔로 출력


